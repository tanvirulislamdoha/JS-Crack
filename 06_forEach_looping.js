                            //* forEach loop
                            //* forEach loop
                            //* forEach loop
                            //* forEach loop


const arr = [1, 2, 3, 4, 5, 6, 'tanvir', 'islam', 'doha']

const obj = {
    id : 4321,
    name: 'tanvir islam',
    email: 'tanvir@io.com',
    address: 'sylhet'
}

const str = "hello world"

//! forEach() shudu matro array te use kora jay

// str.forEach((st) => console.log(st));

        // arr.forEach((ar) => console.log(ar))
        // arr.forEach((value, index, arraay) => console.log(`${index} : ${value} => returned array: ${arraay}`))

    

        // arr.forEach(arr_var => console.log(arr_var))

        // obj.forEach((obj_val) => console.log(obj_val))

//  you can't use forEach on a object like that way

        // arr.forEach((key, value)=>{
        //     console.log(key, value)
        // })

        // obj.forEach(([value, key]) => console.log(key, value) )


        // arr.forEach((value, key)=>{
        //     console.log(`${key} is ${value}`)
        // })
