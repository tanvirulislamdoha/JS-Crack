        // ! find
        // ! find
        // ! find
        // ! find

    const arr = ["hello", "cat", "dog", "lion"];

        // it returns first value that match with the condition.

        // console.log(arr.find(a => a === "dog"))

        const users = [
            { id: 1, name: 'John' },
            { id: 2, name: 'Alice' },
            { id: 3, name: 'Bob' }
          ];

    const finder = users.find((user) => user.name == 'Alice')
    // console.log(finder)